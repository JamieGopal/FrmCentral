@model IEnumerable<FrmCentral.Models.FarmerProduct>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Farmer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Farmer.FmrId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.PrdtId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FmrProductId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FmrProductId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FmrProductId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
